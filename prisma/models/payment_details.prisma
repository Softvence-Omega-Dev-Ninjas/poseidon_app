enum ServiceType {
    membership
    support
}

model paymentDetails {
    id          String      @id @unique @default(uuid())
    buyerId     String
    sellerId    String
    productName String
    amount      Float
    serviceType ServiceType
    serviceId   String
}
